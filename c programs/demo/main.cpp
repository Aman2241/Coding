#include <stdio.h>
#include <stdlib.h>
#include<algorithm.h>

int main()
{
    int a[10];
    for(int i=0;i<10;i++)
    {
    cin>>a[i];
    }
    sort(a,a+n);
    for(int i=0;i<10;i++)
    {
    cin>>a[i];

}
